#include "main.h"

/**
 * rev_string - A C function that prints a string,
 * in reverse.
 *
 * @s: string to reverse
 */
void rev_string(char *s)
{
	int i;

	for (i = ( _strlen(s) - 1); i >= 0; i--)
	{
		_putchar(s[n]);
	}
}
